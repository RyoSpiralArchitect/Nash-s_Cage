# Artifacts

`reference_run/` contains the committed F0 baseline generated by:

```bash
python3 -m simulation run \
  --config simulation/configs/minimal.json \
  --episodes 64 --seed 7 \
  --out artifacts/reference_run --overwrite
```

Verify it from the repository root:

```bash
make verify-artifact
```

The artifact is a reproducibility fixture and an example of the output contract. It is not a calibrated result, benchmark score, or empirical policy comparison.

To regenerate after an intentional model or configuration change:

```bash
make experiment
```

Review the changed `summary.csv`, `comparison.md`, `resolved_config.json`, and `receipt.json` before committing. A source change invalidates the previous receipt even when numeric CSV outputs happen to remain unchanged.
